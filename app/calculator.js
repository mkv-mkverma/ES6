const addNum = (x, y) =>{
    return x + y;
}
const multiplyNum = (x, y) =>{
    return x * y;
}

// export {addNum , multiplyNum};

export default multiplyNum;

// default can't be used with const, let, var